# Library

This directory preserves reusable DreamNet DreamLoops and Capsule examples from the private implementation history. The content is public reference material, not an automatic grant of authority and not a claim that every named integration is currently deployed.

Machine-executable stable manifests live under `starter-kits/` and are validated by the current runtime. Older human-readable permission names remain preserved here for provenance.
